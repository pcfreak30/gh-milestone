#!/bin/bash
# Bash wrapper script for gh-milestone

# Get the directory where this script is located
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

# Run the Python module from the gh_milestone package
cd "$SCRIPT_DIR"
python3 -m gh_milestone.main "$@"